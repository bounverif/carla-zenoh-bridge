syntax = "proto3"

message VehicleIncoming {

    string name = 1;
    float throttle = 2;
    float brake = 3;
    float steer = 4;
    int32 gear = 5;
    bool handbrake = 6;
    bool reverse = 7;
    bool manual-gear-shift = 8;

}

message Incoming {

    repeated message VehicleIncoming = 1;
}